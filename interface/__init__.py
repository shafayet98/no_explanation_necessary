"""Interface layer — FastAPI REST API + React frontend. Thin: no business logic.

The API exposes POST /query and GET /health. The React frontend (frontend/,
Phase 8) is a pure rendering client of those endpoints.
"""
