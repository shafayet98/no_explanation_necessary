# Dev environment — Terraform root module (STUB).
#
# AWS provider and resources are intentionally not defined yet. The architecture
# (compute / object storage / CDN) must be confirmed before Phase 8. See
# ../../README.md.

# terraform {
#   required_version = ">= 1.6"
#   required_providers {
#     aws = {
#       source  = "hashicorp/aws"
#       version = "~> 5.0"
#     }
#   }
# }

# provider "aws" {
#   region = var.aws_region
# }
