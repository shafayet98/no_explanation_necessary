"""Evaluation harness (Phase 2). Loads eval/test_cases.jsonl, runs each
description through the system, and reports recall@10 and MRR versus the most
recent entry in eval/baselines.json.

Stub for now — the eval gate activates once this is implemented.
"""


def main() -> None:
    raise NotImplementedError(
        "eval harness not implemented yet (skeleton) — built in Phase 2"
    )


if __name__ == "__main__":
    main()
